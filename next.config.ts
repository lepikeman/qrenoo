import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: {
    domains: [
      // Ajoute ton domaine Supabase ici (remplace par le tien si besoin)
      'lgxcufxbvvedftlcxoqp.supabase.co'
    ],
  },
  /* config options here */
};

export default nextConfig;
